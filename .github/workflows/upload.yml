name: 'Upload'
on: workflow_dispatch
jobs:

  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Upload artifact
        run: echo hello > world.txt
        uses: actions/upload-artifact@v3
        with:
          name: test_{{ github_sha }}
          path: world.txt
